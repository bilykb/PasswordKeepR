<nav>
  <h4>Shenanigans</h4>
  <div class="nav_right">
    <% if (email) { %>
      <span><%=email%></span>
    <% } %>

    <form style="display: inline; margin-left: 1em" class="logout_form" method="POST" action="/api/user/logout">
      <button class="logout_btn btn btn-secondary" class="btn btn-secondary">
        Log Out
      </button>
    </form>
  </div>
</nav>
