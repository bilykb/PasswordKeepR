<nav>
  <h4>Shenanigans</h4>
  <div class="nav_right">
    <% if (private) { %>
      <span><%=private[0].email%></span>
    <% } %>

    <form style="display: inline; margin-left: 1em" class="logout_form" method="POST" action="/user/logout">
      <button class="logout_btn btn btn-secondary" class="btn btn-secondary">
        Log Out
      </button>
    </form>
  </div>
</nav>
